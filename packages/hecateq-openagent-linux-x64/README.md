# @hecateq/hecateq-openagent-linux-x64

Platform-specific binary for `@hecateq/hecateq-openagent` — Linux x64 (glibc).

This package is installed automatically as an optional dependency of `@hecateq/hecateq-openagent`. You should not install it directly.
